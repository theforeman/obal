---
- name: Release packages
  hosts:
    - packages
  serial: 1
  gather_facts: false
  roles:
    - role: build_package
      when: (diff_package_changed is not defined or diff_package_changed|bool)
