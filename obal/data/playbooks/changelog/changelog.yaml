---
- hosts:
    - packages
  serial: 1
  gather_facts: false
  roles:
    - spec_file_path
  tasks:
    - name: 'ensure changelog'
      changelog:
        spec: "{{ spec_file_path }}"
        changelog: "{{ changelog | default('- rebuilt') }}"
