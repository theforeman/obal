---
- name: Verify selected Koji tag
  when: tag != 'all'
  include_tasks: "verify.yaml"
  vars:
    tag_name: "{{ tag }}"

- name: Verify all Koji tags
  when: tag == 'all'
  include_tasks: "all.yaml"
