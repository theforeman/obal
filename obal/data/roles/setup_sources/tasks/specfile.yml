---
- name: 'Extract Sources from spec'
  specfile_sources:
    spec_file: "{{ spec_file_path }}"
  register: setup_sources_sources

- name: Configure source in Git Annex
  include_tasks: annex.yml
  with_items: "{{ setup_sources_sources.sources | list }}"
