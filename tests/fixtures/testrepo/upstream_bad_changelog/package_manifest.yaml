---
packages:
  vars:
    releasers:
      - dist-git
    diff_package_skip: false
    diff_package_type: 'koji'
    diff_package_koji_command: 'koji'
    koji_tags:
      obaltest-nightly-rhel7:
        scl: tfm
  hosts:
    hello: {}

repoclosures:
  hosts:
    core-repoclosure: {}
