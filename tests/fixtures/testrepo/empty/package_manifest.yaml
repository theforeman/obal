---
packages:
  vars:
    diff_package_skip: false
    build_package_koji_command: 'brew'
    releasers:
      - obaltest-dist-git-rhel-7
  hosts:
    hello:
      upstream: "../../upstream/.git"
      branch: "master"
      upstream_files:
        - "packages/hello/"
